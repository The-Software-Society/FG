# Streamlit secrets — copy this file to `secrets.toml` (gitignored) and edit.
# Streamlit Community Cloud users: paste the contents into Settings → Secrets.

# 5-character password that gates the model UI.
password = "tss26"
